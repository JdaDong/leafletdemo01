# TypeScript 地图开发 TodoList 🗺️

> 基于当前 Leaflet 项目，可以尝试的有趣方向，从简单到高级排列。

---

## 🎨 一、视觉炫酷类

- [x] **热力图 (Heatmap)** ✅ 已完成（`leaflet.heat`）
  - 展示数据密度，比如人流、犯罪率、房价分布
  - 库：`leaflet.heat`、`heatmap.js`
  - 场景：外卖订单热力、打卡热度图
  - **已实现特性**：
    - 🎛 图层控件勾选 **🔥 POI 热力图**，与 POI 搜索结果联动自动填充热点
    - 🔁 **多页批量抓取**：右上角独立控件，可选 3 / 5 / 10 / 20 页一次性拉取并聚合（每页 25 条，最多 ~500 个热点）
    - 📏 **缩放自适应**：监听 `zoomend`，`radius` / `blur` 按 zoom 动态计算（公式 `radius ≈ 14 + (zoom-10)×3.25`，范围 10~50px）
    - 🎨 渐变配色：蓝 → 绿 → 黄 → 橙 → 红，密集区自然变红
    - ♻️ 复用 `searchPOI` 同一套参数规则（`keyword / mode / city / polygon`），支持 nearby / city / bbox 三种搜索模式

- [x] **轨迹动画回放** ✅ 已完成（`leaflet-ant-path` + 自实现）
  - 让小车 / 飞机 / 轮船沿路径动起来
  - 库：`leaflet-ant-path`（蚂蚁线）、`leaflet-moving-marker`
  - 场景：物流追踪、航班轨迹、运动轨迹回放
  - **已实现特性**：
    - 🐜 **蚂蚁线**：在驾车/步行/骑行路线上叠加流动虚线动画
    - ▶️ **播放面板**：播放 / 暂停 / 重放 + 四档速度（60/200/600/1500 m/s）
    - 🚗 **Emoji 跟随标记**：按出行模式自动切换 🚗/🚶/🚴
    - 📊 **实时进度**：显示已走百分比
    - 📷 **相机跟随**：可选模式，地图自动 panTo 跟着 marker 走（像导航软件）
    - 🕰 **已走路径变灰**：实时绘制灰色叠加线，直观区分已行/待行部分就像 GPS 历史轨迹

- [ ] **3D 地图**
  - 平面地图不过瘾？上 3D！
  - 库：`Cesium`（数字地球）、`Mapbox GL JS`、`deck.gl`
  - 玩法：3D 建筑、地形起伏、太空视角

- [ ] **粒子 / 风场图**
  - 展示风向、洋流、迁徙路线
  - 库：`leaflet-velocity`、`windy-leaflet-plugin`
  - 场景：气象可视化（像 Windy.com 那种）

---

## 🛠 二、交互功能类

- [x] **地图绘制工具** ✅ 已完成（`leaflet-geoman` + `@turf/turf`）
  - 让用户画点、画线、画多边形
  - 库：`leaflet-draw`、`leaflet-geoman`
  - 场景：圈选区域统计、自定义配送范围
  - **已实现特性**：
    - 🖍 左侧绘制工具栏：点/线/矩形/多边形/圆 → 自由变换 + 拖拽编辑 + 删除
    - 📐 **自动分析**：画完徒手即的面积 / 周长 / 中心点（turf.js 算）
    - 📍 **POI 计数**：自动统计落在多边形内的 POI 数量 + Top 3 分类
    - ➕ **缓冲区**：一键为任意点/线/面生成 N 米缓冲区（turf.buffer）
    - 🗑 一键清空所有绘制图层
    - 💾 **持久化**：绘制的所有图形自动存入 localStorage，刷新页面自动恢复
    - 🏷️ **Popup 交互**：点击任何图形 → 看名称/面积/长度等信息，可重命名 ✅、换颜色 ✅、一键定位 ✅（名称同步进 localStorage）

- [ ] **路径规划 / 导航**
  - A 点到 B 点最短路径
  - 库：`leaflet-routing-machine` + OSRM / 高德 API
  - 玩法：多点途经、避开拥堵

- [ ] **聚合标记 (Cluster)**
  - 几万个点也不卡
  - 库：`leaflet.markercluster`、`supercluster`
  - 场景：全国门店分布、POI 展示

- [ ] **测量工具**
  - 测距离、测面积
  - 库：`leaflet-measure`、`turf.js`（几何计算神器）

---

## 🌍 三、数据可视化类

- [x] **Choropleth 分级着色图** ✅ 已完成（纯 Leaflet GeoJSON 实现）
  - 按省 / 市数据染色（GDP、人口、疫情等）
  - 库：Leaflet + `d3.js` + GeoJSON
  - 效果：肉眼看出地区差异
  - **已实现特性**：
    - 🗺 🇨🇳 **真实边界**：一键从DataV加载省级精确边界（约 2MB），首次下载后自动缓存到 localStorage（24h 有效）
    - 🎨 或使用 34 省 demo 菱形数据（无需联网）
    - 📊 7 级 YlOrRd 色带，等分位数自动划分阈值
    - 🔄 **字段切换**：hover 高亮 + 点击缩放，可在 `value` / `per_capita` 两个指标间切换
    - 🧾 右下角自动生成图例，与数值范围同步
    - 📊 **真实 GDP**：一键加载中国 31+ 省 2023 年真实 GDP（亿元）、人均 GDP 数据，自动匹配名称，在真实边界上染色

- [x] **OD 飞线图 / 迁徙图** ✅ 已完成（弧形贝塞尔 + 蚂蚁线自实现）
  - 城市之间的连线动画，百度迁徙那种
  - 库：`echarts-gl`、`deck.gl` 的 ArcLayer
  - 场景：春运迁徙、资金流向
  - **已实现特性**：
    - 🗺️ 二次贝塞尔曲线生成弧线，叠加蚂蚁线流动动画
    - 🔴 颜色 + 粗细按 volume 映射（14 个预设城市、春运风格 demo 12 条）
    - 🎯 起点终点带 emoji 圈点 + 带色名牌
    - ➕ 支持手动输入起/讫/流量 添加任意 OD

- [x] **时间轴地图** ✅ 已完成（`leaflet-timedimension`）
  - 随时间变化的动态地图
  - 库：`leaflet-timedimension`
  - 场景：疫情传播、历史事件演变
  - **已实现特性**：
    - 📅 内置 2020 年 12 个月的扩散 demo 数据（以上海为中心逐月漫延）
    - ⏱ 右下角时间轴控件：播放/暂停/拖动/循环 + 速度滑块
    - 🎨 以颜色分级呈现 value（黄 → 橙 → 红），点击弹 popup 显示详细指标
    - ▶️ 点击 "开始演示" 启动、"停止" 清除所有时间图层

---

## 🚀 四、硬核进阶类

- [ ] **矢量瓦片 (Vector Tiles)**
  - 比栅格瓦片更清晰、更灵活
  - 库：`MapLibre GL`、`Mapbox GL`
  - 玩法：自定义地图样式（夜晚风、水墨风、赛博朋克风）

- [ ] **室内地图 / 楼层切换**
  - 商场、机场多楼层导航
  - 库：`leaflet-indoor`

- [ ] **AR 地图**
  - 手机摄像头 + 地图（类似 Pokemon GO）
  - 库：`AR.js` + Leaflet

- [ ] **地图游戏化**
  - 🎮 **GeoGuessr 复刻**：猜地点小游戏
  - 🗺️ **抢地盘**：类似开心消消乐的地理版
  - 🏃 **LBS 寻宝**：走到真实地点解锁

---

## 💡 推荐优先级

| 优先级 | 推荐 | 理由 |
|:---:|:---:|:---|
| ⭐⭐⭐⭐⭐ | 热力图 + 聚合标记 | 数据多的时候非常实用 |
| ⭐⭐⭐⭐ | 蚂蚁线轨迹动画 | 效果炫、代码少 |
| ⭐⭐⭐⭐ | turf.js 几何计算 | 点在多边形内、缓冲区、最近邻、凸包、泰森多边形 ✅ 已完成 |

---

## 🆕 新增模块

### 🧠 Turf 高级几何分析 ✅ 已完成
- 面板位置：地图左下角，**基于最近一次 POI 搜索结果**
- 🔗 **最近邻**：每个 POI 连向其最近的另一个 POI（青色虚线）
- 📐 **凸包 Convex Hull**：用最小凸多边形包住所有点，自动计算范围面积
- 🧩 **泰森多边形 Voronoi**：按"离哪个 POI 最近"把平面切成彩色区域（服务覆盖/最近门店分析标配）

### 📥 CSV 拖入数据可视化 ✅ 已完成
- 直接把 `province,gdp,population,...` 格式的 CSV 拖到地图任意位置
- 自动加载真实省界 + 按省名容错匹配 + 注入字段下拉框
- 支持任意数量的数值列（GDP、人口、人均 GDP、生育率……都行）

### 📈 多年份 GDP 时间轴 ✅ 已完成
- 内嵌 31 省 + 港澳台 6 年（2018-2023）真实 GDP 数据
- 一键启动后右下角出现时间轴控件，可播放/暂停/拖拽
- 顶部居中大字显示当前年份，全局色阶保证年份切换可比
- 鼠标悬停看具体省份当年 GDP

### 🚄 OD 数据时间轴 ✅ 已完成
- 12 个月不同流量的春运动态：1/2 月春运 ×1.6-1.8、7/8 月暑运 ×1.4-1.5、10 月国庆 ×1.3、12 月年终 ×1.5
- 顶部彩色月份标签（带 emoji）+ 右下角时间轴
- 颜色 + 粗细随当月流量动态变化

### 🗂 绘制图形分组管理（左侧抽屉）✅ 已完成
- 左上 🗂 按钮唤起左侧抽屉
- 列出所有 drawnLayers（带类型、名称、关键指标）
- hover 自动高亮对应图形（橙色 5px）
- ✅ 全选 / ✏️ 批量重命名（支持 `{n}` `{name}` 模板）/ 🗑 批量删除
- 📤 一键导出为 GeoJSON 文件
| ⭐⭐⭐ | 迁移到 MapLibre GL | 解锁 3D + 自定义样式 |

---

# 📋 尚未实现功能盘点（2026-05-11 更新）

## 🎨 一、视觉炫酷类（剩 2 项）

### ❌ 3D 地图
- **Cesium** 数字地球：地形起伏、3D 建筑、太空视角
- **Mapbox GL JS / MapLibre GL**：3D extrude buildings
- **deck.gl HexagonLayer**：3D 蜂巢热力图

### ❌ 粒子 / 风场图
- 库：`leaflet-velocity`
- 数据：NOAA 公开气象数据
- 效果：像 Windy.com 那种风向 / 洋流流动粒子

---

## 🛠 二、交互功能类（剩 3 项）

### ❌ 路径规划高级特性
> 注：基础驾车 / 步行 / 骑行已实现，但还缺：
- **多点途经**（A → B → C → D 一次规划）
- **避开拥堵 / 避开收费**（高德 strategy 切换）
- **路线偏好对比**（同时显示 3 条候选路线让用户选）

### ❌ 聚合标记进阶
> 注：基础 markercluster 已实现，但还能玩：
- **超大数据集**（1 万+ 点用 `supercluster`，比 markercluster 快 10x）
- **服务端聚合**（按 z/x/y 让后端预聚合）

### ❌ 测量工具
- 库：`leaflet-measure`
- 测距离、测面积、测方位角
- > 注：用 Geoman 画线 / 多边形可以分析得到面积 / 长度，但没有"测量"专用工具的连续点击交互

---

## 🚀 三、硬核进阶类（剩 4 项）

### ❌ 矢量瓦片 (Vector Tiles)
- 库：`MapLibre GL JS`、`Mapbox GL JS`
- 玩法：**自定义地图样式**（夜晚风 / 水墨风 / 赛博朋克风 / 极简灰）
- 比栅格瓦片更清晰、文字不糊、可任意旋转

### ❌ 室内地图 / 楼层切换
- 库：`leaflet-indoor`
- 商场 / 机场 / 写字楼，按楼层切换 GeoJSON

### ❌ AR 地图
- 库：`AR.js` + Leaflet
- 手机摄像头 + 地图（类似 Pokemon GO）

### ❌ 地图游戏化
- 🎮 **GeoGuessr 复刻**：随机投放到一个街景，让用户猜地点
- 🗺️ **抢地盘**：把全国按网格切，每天能"占领"一个格子
- 🏃 **LBS 寻宝**：在真实地点埋藏宝箱，用户走到附近解锁

---

## 🆕 四、还能继续深挖的方向（基于已有功能）

| 类别 | 具体玩法 | 价值 |
|:---|:---|:---|
| **数据可视化** | GDP 增长率视图（按 (本年/上年-1) 着色） | 揭示真实经济活力，与总量结论常常不同 |
| **数据可视化** | 省份排名"赛跑图"（数据大屏经典效果） | 故事感超强 ⭐⭐⭐⭐⭐ |
| **数据可视化** | 双轴对比图（GDP × 人口、GDP × 面积） | 看人均 / 单位面积差异 |
| **POI 分析** | DBSCAN 聚类（识别"商圈" / 热点区） | 比 markercluster 更智能 |
| **POI 分析** | KDE 核密度估计（更专业的热力图替代） | 学术风、报告风 |
| **绘制工具** | 抽屉里搜索 + 过滤 + 分组（图形多了之后必备） | 实用 |
| **绘制工具** | 撤销 / 重做（Ctrl+Z）支持 | 体验升级 |
| **绘制工具** | 图形导入（拖入 GeoJSON / KML / GPX） | 闭环导入导出 |
| **轨迹动画** | 多轨迹同时回放（多车物流、车队管理） | 物流场景刚需 |
| **路径规划** | 路径上的 POI 提示（沿途加油站 / 服务区） | 自驾游必备 |
| **CSV 拖入** | 模板下载 + 错误行红字标注 + 列映射弹窗 | 大幅降低使用门槛 |
| **协作** | 把绘制图形分享出去（生成短链 / 二维码） | 团队场景刚需 |
| **离线** | Service Worker 缓存瓦片 → 离线可用 | 出差 / 低网场景 |
| **导出** | 把当前地图视图导出为 PNG / PDF | 报告制作 |

---

## 💡 推荐优先级（按"性价比"排序）

| 优先级 | 推荐 | 理由 |
|:---:|:---|:---|
| ⭐⭐⭐⭐⭐ | **GDP 排名赛跑图** | 数据现成、效果惊艳、代码量少 |
| ⭐⭐⭐⭐⭐ | **粒子风场图** | 视觉震撼，是项目"封面级"功能 |
| ⭐⭐⭐⭐ | **多点途经路径规划** | 路径模块的自然延伸，刚需 |
| ⭐⭐⭐⭐ | **MapLibre GL 矢量瓦片 + 自定义样式** | 给项目换个皮，立刻"高级感"拉满 |
| ⭐⭐⭐⭐ | **GeoGuessr 复刻**（小游戏） | 趣味性极高，分享传播率最高 |
| ⭐⭐⭐ | **测量工具** | 经典刚需，但 Geoman 已部分覆盖 |
| ⭐⭐⭐ | **导入 GeoJSON / KML / GPX** | 与导出形成闭环 |
| ⭐⭐ | **3D 地图（Cesium）** | 投入大，本质上是换库 |
| ⭐⭐ | **AR 地图** | 演示效果好，但实用性低 |
| ⭐⭐ | **室内地图** | 缺数据源，不好演示 |

---

## 🎯 三套打包推荐

- 🥇 **"赛跑图 + 风场图 + 多点途经" 三件套**（数据可视化方向继续深耕）
- 🥈 **"MapLibre 矢量瓦片 + 自定义夜晚风"**（视觉换代）
- 🥉 **"GeoGuessr 小游戏"**（趣味方向）